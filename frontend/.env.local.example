# Local dev — Next.js rewrites /api to FastAPI
API_URL=http://127.0.0.1:8000

# Production (Vercel) — browser calls Railway directly (set in Vercel dashboard)
# NEXT_PUBLIC_API_URL=https://your-app.up.railway.app
