# FastAPI backend URL (used by Next.js dev server rewrites)
API_URL=http://127.0.0.1:8000

# Optional: set when the frontend calls the API from a different origin
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
